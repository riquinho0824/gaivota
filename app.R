# app.R

if(F){

  rsconnect::deployApp(
    appDir  = ".",
    appName = "gLnvDsQwdwFk"
  )

}

# library(gaivotapp)
#pkgload::load_all(export_all = FALSE, helpers = FALSE)
# run_app()

# print(getwd())

# pkgload::load_all(export_all = FALSE, helpers = FALSE)
# shinyApp(ui = app_ui, server = app_server)

# library(gaivotapp)
# run_app()

